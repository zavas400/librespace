{
  "name": "ipfs-blockchain-implementation",
  "version": "1.0.0",
  "description": "This is a web app that allows the user to download files from the IPFS network that are valitated through blockchain",
  "main": "index.js",
  "scripts": {
    "start": "node .",
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "nodemon ."
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.19.2",
    "nodemon": "^3.1.0"
  }
}
